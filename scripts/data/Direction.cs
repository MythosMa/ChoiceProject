/// <summary>
/// 输入方向枚举（9方向，含中立）。
/// 属于数据结构，供 InputManager / ComboSystem / UI 等多方使用。
/// </summary>
public enum Direction
{
    Neutral,
    Up,
    Down,
    Left,
    Right,
    UpLeft,
    UpRight,
    DownLeft,
    DownRight
}
